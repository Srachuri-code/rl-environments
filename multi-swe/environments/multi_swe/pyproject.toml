[project]
name = "multi-swe"
description = "Multi-SWE RL Environment with OpenHands agent harness"
tags = ["multi-swe", "openhands", "train", "eval", "multilingual"]
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "verifiers>=0.1.8",
    "prime-sandboxes>=0.2.5",
    "datasets>=2.14.0",
    "multi-swe-bench",
    "tenacity>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["multi_swe.py"]

[tool.uv.sources]
multi-swe-bench = {git = "https://github.com/multi-swe-bench/multi-swe-bench.git", rev = "e1c2bb6"}
